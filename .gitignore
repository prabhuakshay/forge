__pycache__/
*.pyc
.ruff_cache/
.mypy_cache/
.pytest_cache/
.venv/
.coverage

# Claude Code local session/scheduling state (not plugin config — that's in
# .claude-plugin/). Must never be committed.
.claude/

# forge: runtime state and one-shot override flags are local, not shared.
.forge/state.json
.forge/override-*
