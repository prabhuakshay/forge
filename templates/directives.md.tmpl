# Binding directives — {{PROJECT_NAME}}

Durable, agreed constraints for this project. Every contributor and agent obeys
them without exception. Each links to the decision record that explains why.

Add to this file only via `/forge:decide` (which also writes the matching ADR in
docs/decisions/). Keep each line a single, testable imperative.

<!-- directives are appended below this line -->
