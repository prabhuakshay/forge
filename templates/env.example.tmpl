# Copy to `.env` and fill in real values: `cp .env.example .env`
# /forge:audit enforces that every env var read in code appears here. Keep this
# list complete and the placeholders safe (never commit real secrets).
#
# Format: KEY=placeholder   # one-line description of what it's for

# Example (delete once you add real config):
# DATABASE_URL=postgresql://user:pass@localhost:5432/dbname   # primary datastore
