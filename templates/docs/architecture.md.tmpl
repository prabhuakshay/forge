# Architecture

> Keep this current. `/forge:review` flags changes that add a new top-level
> module or service not described here, and `/forge:audit` checks that the paths
> and components named below still exist.

## Overview

_One paragraph: what this project does and the shape of its solution._

## Components

| Component | Location | Responsibility |
|-----------|----------|----------------|
| _name_    | `src/{{PACKAGE}}/...` | _what it owns_ |

## Data & control flow

_How a request / invocation moves through the system. Replace with a real
description as the project takes shape._

## Key constraints

_Cross-references to [decisions](decisions/README.md) that shape this design._
